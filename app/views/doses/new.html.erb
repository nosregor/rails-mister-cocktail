<h1>Add new dose</h1>


<%= form_for([@cocktail, @dose]) do |f| %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :ingredient %>

<%# Alternative method%>
  <%#= f.select :ingredient , @ingredients_and_ids %>


  <%= f.collection_select :ingredient_id, @ingredients, :id ,:name %>

  <% f.text_field :cocktail %>

  <%= f.submit %>
<% end %>
